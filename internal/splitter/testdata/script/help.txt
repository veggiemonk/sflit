# No args shows help and exits 2.
! exec sflit
stderr 'moves Go declarations between files'
stderr 'Usage:'
stderr 'Selection rules:'
stderr 'Examples:'
stderr 'Exit codes:'

# -h shows help and exits 0.
exec sflit -h
stderr 'moves Go declarations between files'

# --help shows help and exits 0.
exec sflit --help
stderr 'moves Go declarations between files'

# help subcommand shows help and exits 0.
exec sflit help
stderr 'moves Go declarations between files'
